sudo: false

git:
  depth: 3

branches:
  only:
    - next
    - master

language: node_js

node_versions: &versions
  - lts/*
  - node

os:
  - linux
  # - windows

cache:
  npm: true

jobs:
  include:
    # - stage: lint ✨
    #   script: npm run lint

    - stage: test 👩🏽‍💻
      script: npm test:cov
      node_js: *versions
